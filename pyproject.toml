[project]
name = "arxiv-translate-skill"
version = "0.1.0"
description = "Project-coupled agent skill for translating arXiv papers into side-by-side Simplified Chinese bilingual PDFs."
requires-python = ">=3.12,<3.13"
dependencies = [
    "BabelDOC==0.6.3",
]

[tool.uv]
package = false
